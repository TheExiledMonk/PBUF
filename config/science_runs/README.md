# Science-run support config

Shared defaults and helper config fragments that multiple science-run scenarios
depend on should be staged here. Individual runnable scenarios now live under
`scenarios/`, but anything reusable across runs (budget templates, reporting
toggles, etc.) belongs in `config/science_runs/`.
